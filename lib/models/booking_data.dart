// Assuming this model is already in 'booking_data.dart'
class BookingData {
  final String month;
  final int bookings;
  final int yearlyTotal;

  BookingData(this.month, this.bookings, this.yearlyTotal);
}